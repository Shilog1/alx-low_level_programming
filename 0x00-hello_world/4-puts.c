#include <stdio.h>
/**
* main - prints a string to standard output using puts function
* Return: always 0
*/
int main(void)
{
		puts("\"Programming is like building a multilingual puzzle");
			return (0);
}
